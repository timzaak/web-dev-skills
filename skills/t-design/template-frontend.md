# [方案名称] 前端技术设计

**生成时间**: [自动生成时间戳]
**状态**: Draft
**主文档**: `.ai/design/[feature].md`

> 本文档是前端分端设计，以用户体验流为主，页面组件结构与状态分工只做承载体验所需的最小技术映射；目标、范围与跨端决策以主文档为准。API 契约以后端分端设计为唯一来源，本文档只声明依赖。

## 1. 目标与范围（前端）

- 承接主文档目标的前端边界: [摘要]
- 不包含: [范围外项]

## 2. 需求来源与决策追踪

- 需求来源: [用户故事 / PRD / 技术预研路径引用，见主文档 §2]
- 关键 UX 期望: [入口、路径、反馈、默认值、错误状态]

| Decision ID | 状态 | 设计落点 | 说明 |
|---|---|---|---|
| `DEC-[feature]-001` | Applied / Not Applicable / Superseded | [章节] | [应用方式或不适用原因] |

## 3. 现有实现分析（前端）

> 只描述现状：现有页面、组件体系、数据流与可复用点。

- `frontend/src/...` - [现有页面/组件]
- 现有数据流: [查询/生成客户端/状态管理现状]
- 可复用点: [已有组件/模式]
- 受影响边界: [需要修改的路由、组件、数据流]

## 4. 用户体验流

> 本节用用户视角语言描述，不展开技术实现；每个关键流程一段。

### 4.1 [流程名，如"创建导出任务"]

- 入口: [用户从哪里进入这个流程]
- 操作路径: [用户每一步做什么]
- 系统反馈: [每步之后用户看到什么]
- 默认值: [表单/列表的默认选择与预填]
- 错误状态与恢复: [失败时用户看到什么、如何重试或纠正]

## 5. 页面与组件设计（最小技术映射）

### 5.1 页面/路由清单

| 路由/页面 | 承载的体验流 | 用途 |
|---|---|---|
| `[route or page]` | [§4 中对应流程] | [用途] |

### 5.2 组件清单与层级

- `[容器组件]` - [职责：数据获取与状态编排]
  - `[展示组件]` - [职责：纯展示]
- 边界说明: [哪些组件持有数据与状态、哪些纯展示；不规定具体 props/state 细节]

### 5.3 页面结构 / 线框说明

```text
[页面]
├── [入口或导航]
├── [主操作区]
└── [反馈、空态或错误区]
```

- 布局与响应式边界: [只写影响体验和组件边界的结构，不写样式实现细节]

## 6. 状态与数据流设计

### 6.1 API 依赖（只引用契约源）

- 契约源: `.ai/design/[feature]/backend.md` §4（或现有接口路径 `[METHOD] /api/...`）

| Operation ID | 方法 | 路径 | 使用的请求字段 | 使用的响应字段 | 用途 |
|---|---|---|---|---|---|
| [operationId] | [METHOD] | `/api/...` | [字段名列表] | [字段名列表] | [用途；不复制字段定义] |

### 6.2 状态分工（TanStack Query / Zustand）

- server-state（TanStack Query）: [哪些数据走查询与缓存、缓存失效与刷新策略；服务端数据只存在于 Query 缓存]
- client-state（Zustand）: [哪些纯 UI 状态（开关、草稿、跨页面交互状态）进哪个 store/slice；跨组件共享服务端数据的选中态时存 ID 引用，不复制数据对象]
- 订阅范围: [组件订阅最小所需状态；对象/数组订阅做浅比较]

## 7. 性能考虑（仅当前必需）

- [代码分割/懒加载/渲染范围；无当前必需项时写"无"并说明]

## 8. 前端测试与 Demo 策略

- 前端测试: [测试入口与覆盖点；遵循 frontend 测试规范]
- `data-testid` 规划: [涉及 Demo/E2E 时列出关键选择器，遵循 testid 规范]
- Playwright Demo 主路径: [主故事验收路径；无需演示时明确说明]

## 9. 详细设计（前端最小实现映射）

- 页面状态模型: [加载、空态、错误、提交中、权限受限之间的转换]
- 关键事件与副作用: [用户事件、mutation、缓存失效、导航或反馈]
- 公共边界: [新增或修改的公开 hook、schema、query key、store action；无则写“无”]

## 10. 风险与验证动作（前端范围）

> 只记录方向已确定的风险和不需要用户选择的验证动作；需要用户裁决的问题必须已在主会话解决。

| 风险项 | 等级 (P0/P1/P2) | 缓解或验证动作 | 负责人 | 完成条件 |
|---|---|---|---|---|
| [风险描述] | [P0/P1/P2] | [动作] | [负责人] | [完成条件] |

## 11. 文件影响范围（前端文件）

> 只列前端文件；全量汇总以主文档 §8 为准。MODIFY/DELETE 路径必须存在；CREATE 路径的父目录必须存在并说明命名依据。

| 文件 | 操作 | 说明 |
|---|---|---|
| `[真实仓库路径]` | CREATE / MODIFY / DELETE | [变更摘要] |
