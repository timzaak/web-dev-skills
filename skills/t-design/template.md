# [方案名称] 技术设计

**生成时间**: [自动生成时间戳]
**状态**: Draft

## 1. 需求概述

### 1.1 方案描述
[简要描述要解决的问题和目标]

### 1.2 目标
- [目标 1]
- [目标 2]

### 1.3 范围

**包含**:
- [范围项]

**不包含**:
- [范围外项]

### 1.4 假设与缺失信息
- [假设或缺失输入]

## 2. 需求来源

### 2.1 用户故事
- `[US-XXX-001]` [标题]，优先级 [P0/P1/P2]，来源 `docs/user-stories/...`
- 场景概述：[简要摘要]
- 核心约束：[约束摘要]

### 2.2 PRD / 其他需求文档
- `.ai/prd/...`（PRD 草稿，如存在）
- `docs/prd/...`（已发布 PRD 基线，如存在）
- 业务规则：[摘要]
- 非功能要求：[摘要]

### 2.3 技术预研
- `.ai/tech-research/...`
- 技术目标：[摘要]
- 选定技术路线：[摘要]
- 业务逻辑变动边界：[涉及 / 不涉及；如不涉及，说明原因]

### 2.4 UI 规格
- `.ai/design-ui/[方案名称]/ui-spec.md`（如存在）
- 承接状态：[基于已确认 UI 规格 / 未发现 UI 规格 / 不涉及前端]
- 选定 UI 方向：[摘要]
- 关键页面与状态：[摘要]

## 3. 现有实现分析

> 本节只描述**现状**：现有代码结构、可复用模块、受影响范围。新增代码骨架放到 §5 详细设计。

### 3.1 后端
- `backend/api/...` - [现有入口/接口]
- `backend/core/...` - [领域逻辑]
- `backend/sdk/...` - [SDK 或对外契约]

### 3.2 前端
- `frontend/src/...` - [页面/组件/数据流]

### 3.3 可复用点
- [已有模块/组件/服务]

### 3.4 受影响范围
- [需要修改的目录、模块、接口]

## 4. 技术方案

### 4.1 总体设计
- [设计思路]
- [关键边界]
- [关键取舍]

### 4.2 API 接口设计

#### 4.2.1 接口清单
| 方法 | 路径 | 用途 | 权限/身份 | 调用方 |
|---|---|---|---|---|
| [GET/POST/PUT/DELETE] | `/api/...` | [用途] | [如 realm admin / user / internal] | [frontend/sdk/internal] |

#### 4.2.2 关键接口详情

**[接口名称]**

- 路径: `[METHOD] /api/...`
- 用途: [该接口解决什么问题]
- 权限: [鉴权与授权要求]
- 请求字段:

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| [fieldName] | [type] | [是/否] | [说明] |

- 响应字段:

| 字段 | 类型 | 说明 |
|---|---|---|
| [fieldName] | [type] | [说明] |

- 错误响应:

| 状态码 | 条件 | 说明 |
|---|---|---|
| 400 | [条件] | [说明] |
| 401/403/404/409 | [条件] | [说明] |

- 备注:
  - 路径参数占位符使用 camelCase，例如 `{realmId}`、`{userId}`
  - 说明新增 DTO、复用 DTO，及与 OpenAPI/SDK 的关系

[不涉及后端接口时，写"不适用"与原因]

### 4.3 数据库设计

#### 4.3.1 设计原则
- 仅设计当前功能必需的表、字段、约束和索引
- 结构尽量简洁，不为未来假设预埋复杂审计方案
- 数据库变更通过迁移文件维护，设计文档说明目标结构与迁移影响

#### 4.3.2 表结构或字段变更

**表: `[table_name]`**

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | `UUID` | `PK` | [说明] |
| `[field_name]` | `[type]` | `[NOT NULL/UNIQUE/FK/DEFAULT]` | [说明] |

- 主键: [主键说明]
- 唯一约束: [约束说明；无则写"无"]
- 必要索引: [索引说明；无则写"无"]
- 外键: [外键说明；无则写"无"]
- 时间字段: [如 `created_at`, `updated_at`]

#### 4.3.3 迁移策略
- 变更类型: [新增表 / 加字段 / 改名 / 删除字段]
- 是否需要数据回填: [是/否 + 说明]
- 兼容性影响: [对现有代码、历史数据、部署顺序的影响]

[不涉及数据库变更时，写"不适用"与原因]

### 4.4 前端设计

> 如存在 `.ai/design-ui/[方案名称]/ui-spec.md`，本节必须标记"基于已确认 UI 规格"，并承接其页面结构、组件映射和关键状态。

#### 4.4.1 页面/组件清单
- `[route or page]` - [用途]
- `[component]` - [用途]

#### 4.4.2 页面线框说明
- 页面区域: [例如筛选区、列表区、详情抽屉、表单弹窗]
- 主要交互: [创建、编辑、删除、提交、切换]
- 关键状态: [加载、空态、错误、提交中、权限受限]
- 数据来源/依赖: [页面依赖哪些查询、配置、生成客户端或现有数据流]

#### 4.4.3 前端实现约束
- [表单模式、查询模式、错误处理、路由承接、数据刷新策略]

[不涉及前端时，写"不适用"与原因]

### 4.5 权限与安全
- [权限模型]
- [鉴权/输入校验/敏感数据处理]

## 5. 详细设计

> 放置具体可被工程师直接照着写代码的设计产物。
> 区别于 §3（描述现状）和 §4（描述方案选择），本节聚焦**新增内容的具体形态**：
> 数据结构完整定义、公开函数签名、错误类型与文案、关键算法或状态机骨架。

### 5.1 [模块名] 设计

**数据结构**:
```rust
// 或 typecript / sql / 其他语言，按目标技术栈
struct Example {
    field_a: String,
    field_b: Option<u32>,
}
```

**公开函数签名**:
```rust
pub fn parse_xxx(input: &[u8]) -> Result<Vec<ParsedChunk>, XxxError>
```

**错误类型与文案**:
```rust
#[derive(Debug, Error)]
enum XxxError {
    #[error("文件编码不支持")]
    InvalidEncoding,
    #[error("第 {index} 条问答缺少必填字段: {fields}")]
    MissingRequiredFields { index: usize, fields: Vec<String> },
}
```

**关键算法/状态机骨架**（如适用）:
```text
.jsonl 文件
  → UTF-8 解码
  → 按行迭代
    → serde_json::from_str
    → 校验必填字段
    → 生成 ParsedChunk
```

### 5.2 [其他模块/接口实现]

[无具体设计产物时写"不适用"与原因]

## 6. 测试策略

### 6.1 单元/集成测试
- [后端测试策略；明确测试文件位置与场景函数名]
- [前端测试策略]

### 6.2 Demo / E2E
- [主故事验收路径]

### 6.3 回归风险点
- [需要重点验证的场景]

## 7. 风险与待确认事项

| 风险项 | 等级 (P0/P1/P2) | 缓解措施 |
|---|---|---|
| [风险描述] | [P0/P1/P2] | [缓解或待确认动作] |
| [需求待确认] | [P0/P1/P2] | [决策动作与负责人] |
| [迁移/兼容性风险] | [P0/P1/P2] | [兼容性策略] |

## 8. 文件影响范围

> 列出全部受影响文件，使用 CREATE / MODIFY / DELETE 操作标记。
> 路径必须为仓库真实路径，不得使用 `backend/src/...` 这类未验证的示例路径。
> `/t-task` 会直接对照本表拆分 item，请保证文件粒度与操作类型准确。

| 文件 | 操作 | 说明 |
|---|---|---|
| `[真实仓库路径]` | CREATE / MODIFY / DELETE | [变更摘要] |

## 9. 外部参考

- 无，基于仓库现有规范与需求资料
- [如人类明确要求补充外部依据，再记录来源标题、链接与结论摘要]

## 10. 下一步
- 检查技术设计：`/t-design-check [方案名称]`
- 如文档较复杂，生成可视化预览：`/t-html-show .ai/design/[方案名称].md`
- 通过 `/t-design-check` 后：`/t-task [方案名称]`
