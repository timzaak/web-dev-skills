# [方案名称] 后端技术设计

**生成时间**: [自动生成时间戳]
**状态**: Draft
**主文档**: `.ai/design/[feature].md`

> 本文档是后端分端设计，聚焦 API 契约、数据模型与领域逻辑；目标、范围与跨端决策以主文档为准。

## 1. 目标与范围（后端）

- 承接主文档目标的后端边界: [摘要]
- 不包含: [范围外项]

## 2. 需求来源与决策追踪

- 需求来源: [用户故事 / PRD / 技术预研路径引用，见主文档 §2]
- 业务规则与非功能要求: [摘要]

| Decision ID | 状态 | 设计落点 | 说明 |
|---|---|---|---|
| `DEC-[feature]-001` | Applied / Not Applicable / Superseded | [章节] | [应用方式或不适用原因] |

## 3. 现有实现分析（后端）

> 只描述现状：现有入口、领域逻辑、可复用点、受影响边界。新增设计放 §9 详细设计。

- `backend/api/...` - [现有入口/接口]
- `backend/core/...` - [领域逻辑]
- `backend/sdk/...` - [SDK 或对外契约]
- 可复用点: [已有模块/服务]
- 受影响边界: [需要修改的模块、接口]

## 4. API 接口设计

> 本章是本方案 API 契约的**唯一设计源**；前端与 Flutter 分端设计只引用本章，不重新定义。

### 4.1 接口清单

| Operation ID | 方法 | 路径 | 用途 | 权限/身份 | 调用方 |
|---|---|---|---|---|---|
| [operationId] | [GET/POST/PUT/DELETE] | `/api/...` | [用途] | [如 realm admin / user / internal] | [frontend/flutter/sdk/internal] |

### 4.2 关键接口详情

**[接口名称]**

- 路径: `[METHOD] /api/...`
- 用途: [该接口解决什么问题]
- 权限: [鉴权与授权要求]
- 请求字段:

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| [fieldName] | [type] | [是/否] | [说明] |

- 响应字段:

| 字段 | 类型 | 说明 |
|---|---|---|
| [fieldName] | [type] | [说明] |

- 错误响应:

| 状态码 | 条件 | 说明 |
|---|---|---|
| 400 | [条件] | [说明] |
| 401/403/404/409/422 | [条件] | [说明] |

### 4.3 DTO 与契约兼容

- 路径参数占位符使用 camelCase，例如 `{realmId}`、`{userId}`
- 新增 DTO: [清单]
- 复用 DTO: [清单与边界]
- 与 OpenAPI/SDK 的关系: [影响说明]
- 版本与兼容策略: [如有破坏性变更必须说明迁移路径]

[不涉及后端接口时，写"不适用"与原因]

## 5. 数据库设计

### 5.1 设计原则

- 仅设计当前功能必需的表、字段、约束和索引
- 结构尽量简洁，不为未来假设预埋复杂审计方案
- 数据库变更通过迁移文件维护，本文档说明目标结构与迁移影响

### 5.2 表结构或字段变更

**表: `[table_name]`**

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| `id` | `UUID` | `PK` | [说明] |
| `[field_name]` | `[type]` | `NOT NULL/UNIQUE/FK/DEFAULT` | [说明] |

- 主键: [说明]
- 唯一约束: [说明；无则写"无"]
- 必要索引: [说明；无则写"无"]
- 外键: [说明；无则写"无"]
- 时间字段: [如 `created_at`, `updated_at`]

### 5.3 迁移策略

- 变更类型: [新增表 / 加字段 / 改名 / 删除字段]
- 是否需要数据回填: [是/否 + 说明]
- 兼容性影响: [对现有代码、历史数据、部署顺序的影响]

[不涉及数据库变更时，写"不适用"与原因]

## 6. 领域逻辑设计

- 核心业务规则与流程: [规则与流程说明]
- 输入校验: [校验规则与失败行为]
- 事务边界: [哪些操作必须在同一事务内]
- 幂等与并发: [重复提交、并发冲突的处理方式]

[不涉及复杂领域逻辑时，写"不适用"与原因]

## 7. 权限与安全

- 权限模型: [谁能做什么]
- 鉴权要求: [接口级鉴权说明]
- 敏感数据处理: [如有]

## 8. 非功能设计（仅当前必需）

- 性能目标: [如有]
- 缓存策略: [如有]
- 可观测性: [如有]

[无当前必需项时写"无"并说明]

## 9. 详细设计

> 放置具体可被工程师直接照着写代码的设计产物：数据结构完整定义、公开函数签名、错误类型与文案、关键算法或状态机骨架。

**数据结构**:
```rust
struct Example {
    field_a: String,
    field_b: Option<u32>,
}
```

**公开函数签名**:
```rust
pub fn parse_xxx(input: &[u8]) -> Result<Vec<ParsedChunk>, XxxError>
```

**错误类型与文案**:
```rust
#[derive(Debug, Error)]
enum XxxError {
    #[error("文件编码不支持")]
    InvalidEncoding,
}
```

**关键算法/状态机骨架**（如适用）:
```text
输入 → 解码 → 校验 → 产出
```

## 10. 后端测试策略

- 单元测试: [测试文件位置与场景函数名；遵循 backend 测试规范]
- 场景测试: [入口与覆盖场景；遵循 backend 测试规范]

## 11. 风险与验证动作（后端范围）

> 只记录方向已确定的风险和不需要用户选择的验证动作；需要用户裁决的问题必须已在主会话解决。

| 风险项 | 等级 (P0/P1/P2) | 缓解或验证动作 | 负责人 | 完成条件 |
|---|---|---|---|---|
| [风险描述] | [P0/P1/P2] | [动作] | [负责人] | [完成条件] |

## 12. 文件影响范围（后端文件）

> 只列后端文件；全量汇总以主文档 §8 为准。MODIFY/DELETE 路径必须存在；CREATE 路径的父目录必须存在并说明命名依据。

| 文件 | 操作 | 说明 |
|---|---|---|
| `[真实仓库路径]` | CREATE / MODIFY / DELETE | [变更摘要] |
